# H2-B01 dedup concurrency test stability

- Entry main: `4753bb4e11677044cd85b2c84eedfac09d9c9fa1`
- Trigger: post-merge main CI `33910892792` failed twice on `dedup::tests::open_critical_section_excludes_recovery_via_activity_guard`.
- Scope: test-only serialization of the two tests that mutate the process-global `PRE_FLOCK_HOOK`.
- Production code, schema, API, executor, and runtime behavior are unchanged.
- The test hook remains compiled only under `cfg(test)`.

The guard prevents the hook-based tests from racing when Rust runs the storage test binary in parallel. This removes the release-gate flake while preserving the original barrier assertions.
